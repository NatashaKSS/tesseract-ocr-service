import { expect } from 'chai';
import { ocr } from '../ocr';

describe('[ocr.js] Test cases', () => {
  it('Should return...', () => {
    // expect(ocr()).to.equal('from the other side!');
  });
});
 